{
  "name": "wx-travel-cloudfunctions",
  "version": "1.0.0",
  "description": "AI旅游路线规划小程序云函数",
  "scripts": {
    "install-all": "cd login && npm install && cd ../generate-route && npm install && cd ../get-routes && npm install && cd ../get-route-detail && npm install && cd ../update-profile && npm install",
    "deploy-all": "echo '请在微信开发者工具中部署云函数'"
  },
  "dependencies": {},
  "devDependencies": {}
}